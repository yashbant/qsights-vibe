# =============================================================================
# QSights Backup System - .gitignore
# =============================================================================
# This file excludes sensitive data from git commits
# The backup scripts themselves SHOULD be committed for version control
# =============================================================================

# Sensitive configuration file with credentials
backup-config.sh

# Database backups (too large and contain sensitive data)
database/*.sql
database/*.dump
database/*.backup

# Environment file backups (contain secrets)
env-files/*.env
env-files/*.env.local
env-files/*.env.production

# Media backups (too large for git)
media/

# Code snapshots (redundant with git)
code-snapshots/*.tar.gz

# Temporary files
*.tmp
*.log
.DS_Store
